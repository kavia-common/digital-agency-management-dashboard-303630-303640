# Backend base URL (used when REACT_APP_API_MODE=real)
REACT_APP_API_BASE_URL=http://localhost:3001

# API mode: 'stub' (default) or 'real'
REACT_APP_API_MODE=stub

# Optional (future): used for Supabase email magic-link redirect
REACT_APP_SITE_URL=http://localhost:3000
